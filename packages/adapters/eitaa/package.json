{
  "name": "@kanal/adapters-eitaa",
  "version": "0.1.0",
  "description": "KANAL Eitaa adapter stub: write-mostly, compile-checked, not shipped (plan §10.8).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test:unit": "vitest run",
    "test": "vitest run"
  },
  "dependencies": {
    "@kanal/adapters-core": "workspace:*",
    "@kanal/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
