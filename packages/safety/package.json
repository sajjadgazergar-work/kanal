{
  "name": "@kanal/safety",
  "version": "0.1.0",
  "description": "KANAL safety: pacing, promotional density, policy classification, autopublish eval, anomaly detection, kill switch, moderation + PII, audit hash chain, human feedback loop, injection corpus.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test:unit": "vitest run",
    "test": "vitest run",
    "test:injection": "vitest run src/__tests__/injection.test.ts"
  },
  "dependencies": {
    "@kanal/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "eslint": "^9.17.0"
  }
}
