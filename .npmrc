# Strict node_modules catches undeclared imports (per plan §18.2)
node-linker=isolated
# No postinstall scripts permitted (per plan §16.2 #13)
enable-pre-post-scripts=false
auto-install-peers=true
strict-peer-dependencies=true
