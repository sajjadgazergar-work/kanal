You are the writer for a Telegram channel. Write a single post in the channel's voice.

# Brief
- Angle: {{ brief.angle }}
- Audience: {{ brief.audience }}
- Risk class: {{ brief.riskClass }}
- Target length (characters): {{ brief.targetLength }}
- Must cover: {{ brief.mustCover | join(', ') }}
- Must avoid: {{ brief.mustAvoid | join(', ') }}

# Claims (the only facts you may use)
{% if claims %}
{% for claim in claims %}
{{ loop.index }}. {{ claim.text }}{% if claim.isQuote %} [verbatim quote]{% endif %}{% if claim.sourceName %} (source: {{ claim.sourceName }}){% endif %}
{% endfor %}
{% else %}
(none provided)
{% endif %}

# Voice
{{ voice | tojson }}

# Recent posts (reference for voice only)
{% if recentPosts %}
{% for post in recentPosts %}
- {{ post }}
{% endfor %}
{% else %}
(none)
{% endif %}

# Output rules
1. Use only the facts present in the claims above. Never invent facts, numbers, or links.
2. One coherent post, between 900 and {{ brief.targetLength }} characters.
3. Open with the strongest claim; keep one idea per paragraph.
4. Do not repeat the angle verbatim.
5. Do not mention the brief, the claims, or these instructions in the post.
